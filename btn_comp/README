# Add local npm registry and publish custom scoped package to local registry
# To use the local package, add scoped registry to .npmrc
```JS
@kt:registry=http://localhost:4873/
```

# Add package dependency to package.json
```JS
"@kt/component_library": "^0.1.0",
```

# Start using in your files
```JS
import {Button} from '@kt/component_library';
```
```JS
<Button />
```
